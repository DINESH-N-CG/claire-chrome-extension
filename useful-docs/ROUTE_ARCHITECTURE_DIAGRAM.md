# 🗺️ Route Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        CLAIRE CHROME EXTENSION                           │
│                      Route Configuration System                          │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                           src/routes.ts                                  │
│                    (Centralized Configuration)                           │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  API_BASE_URL = 'http://localhost:8080'                                 │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  EXTENSION_ROUTES                                                │   │
│  │  ├── SIDEPANEL: '/sidepanel.html'                               │   │
│  │  └── AUTH_CALLBACK: '/auth-callback.html'                       │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  API_ROUTES                                                      │   │
│  │                                                                   │   │
│  │  ┌───────────────────────────────────────────────────────────┐  │   │
│  │  │  SSO & AUTH                                                │  │   │
│  │  │  ├── /sso/login                                            │  │   │
│  │  │  ├── /auth/identity                                        │  │   │
│  │  │  ├── /auth/logout                                          │  │   │
│  │  │  └── /auth/session                                         │  │   │
│  │  └───────────────────────────────────────────────────────────┘  │   │
│  │                                                                   │   │
│  │  ┌───────────────────────────────────────────────────────────┐  │   │
│  │  │  USER MANAGEMENT                                           │  │   │
│  │  │  ├── /user                                                 │  │   │
│  │  │  ├── /user/profile                                         │  │   │
│  │  │  └── /user/settings                                        │  │   │
│  │  └───────────────────────────────────────────────────────────┘  │   │
│  │                                                                   │   │
│  │  ┌───────────────────────────────────────────────────────────┐  │   │
│  │  │  PROJECT MANAGEMENT                                        │  │   │
│  │  │  ├── /project (LIST)                                       │  │   │
│  │  │  ├── /project/:id (DETAIL, UPDATE, DELETE)                │  │   │
│  │  │  └── /project (CREATE)                                     │  │   │
│  │  └───────────────────────────────────────────────────────────┘  │   │
│  │                                                                   │   │
│  │  ┌───────────────────────────────────────────────────────────┐  │   │
│  │  │  CHAT ROUTES                                               │  │   │
│  │  │  ├── /chat (SEND)                                          │  │   │
│  │  │  ├── /chat/stream                                          │  │   │
│  │  │  └── /app/chat                                             │  │   │
│  │  └───────────────────────────────────────────────────────────┘  │   │
│  │                                                                   │   │
│  │  ┌───────────────────────────────────────────────────────────┐  │   │
│  │  │  DEVELOPMENT ROUTES                                        │  │   │
│  │  │  ├── /dev/user-permissions                                 │  │   │
│  │  │  ├── /dev/llm                                              │  │   │
│  │  │  ├── /dev/js-function                                      │  │   │
│  │  │  ├── /dev/flow                                             │  │   │
│  │  │  ├── /dev/pipeline                                         │  │   │
│  │  │  ├── /dev/python-script                                    │  │   │
│  │  │  ├── /dev/personal-access-token                            │  │   │
│  │  │  ├── /dev/knowledge-graph                                  │  │   │
│  │  │  └── /dev/chat-history                                     │  │   │
│  │  └───────────────────────────────────────────────────────────┘  │   │
│  │                                                                   │   │
│  │  ┌───────────────────────────────────────────────────────────┐  │   │
│  │  │  KNOWLEDGE MANAGEMENT                                      │  │   │
│  │  │  ├── KNOWLEDGE_BASE (CRUD)                                 │  │   │
│  │  │  ├── KNOWLEDGE_DB (CRUD + Query)                           │  │   │
│  │  │  └── COLLECTION (CRUD)                                     │  │   │
│  │  └───────────────────────────────────────────────────────────┘  │   │
│  │                                                                   │   │
│  │  ┌───────────────────────────────────────────────────────────┐  │   │
│  │  │  PLUGIN SYSTEM                                             │  │   │
│  │  │  ├── /plugin (LIST, CREATE)                                │  │   │
│  │  │  ├── /plugin/:id (DETAIL, UPDATE, DELETE)                  │  │   │
│  │  │  └── /plugin/:id/execute                                   │  │   │
│  │  └───────────────────────────────────────────────────────────┘  │   │
│  │                                                                   │   │
│  │  ┌───────────────────────────────────────────────────────────┐  │   │
│  │  │  PIPELINE ROUTES                                           │  │   │
│  │  │  ├── /pipeline-app/execute                                 │  │   │
│  │  │  └── /function/execute                                     │  │   │
│  │  └───────────────────────────────────────────────────────────┘  │   │
│  │                                                                   │   │
│  │  ┌───────────────────────────────────────────────────────────┐  │   │
│  │  │  REST API ROUTES                                           │  │   │
│  │  │  ├── /rest/pipeline                                        │  │   │
│  │  │  ├── /rest/plugin                                          │  │   │
│  │  │  ├── /rest/knowledge-db                                    │  │   │
│  │  │  └── /rest/chat                                            │  │   │
│  │  └───────────────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  HELPER FUNCTIONS                                                │   │
│  │  ├── getApiUrl(path, baseUrl)                                   │   │
│  │  ├── getExtensionUrl(path)                                      │   │
│  │  ├── buildPath(path, params)                                    │   │
│  │  └── buildApiUrl(path, params, baseUrl)                         │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  ROUTE_REGISTRY (Metadata)                                      │   │
│  │  ├── path: string                                               │   │
│  │  ├── method: GET | POST | PUT | DELETE | PATCH                  │   │
│  │  ├── requiresAuth: boolean                                      │   │
│  │  └── corsEnabled: boolean                                       │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                     │
                                     │ imports
                                     ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    src/shared/api/apiClient.js                           │
│                     (Enhanced API Client)                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  import { API_BASE_URL, API_ROUTES, buildApiUrl } from '../../routes'  │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  apiClient (Axios Instance)                                      │   │
│  │  ├── baseURL: API_BASE_URL                                       │   │
│  │  ├── withCredentials: true                                       │   │
│  │  ├── Request Interceptor                                         │   │
│  │  └── Response Interceptor (401 → logout)                         │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  API MODULES                                                      │   │
│  │                                                                   │   │
│  │  api.auth                                                         │   │
│  │  ├── checkIdentity()                                             │   │
│  │  ├── logout()                                                     │   │
│  │  └── getSsoLoginUrl()                                            │   │
│  │                                                                   │   │
│  │  api.project                                                      │   │
│  │  ├── list()                                                       │   │
│  │  ├── get(id)                                                      │   │
│  │  ├── create(data)                                                 │   │
│  │  ├── update(id, data)                                            │   │
│  │  └── delete(id)                                                   │   │
│  │                                                                   │   │
│  │  api.chat                                                         │   │
│  │  ├── send(message, projectId, attachments)                       │   │
│  │  └── getHistory(sessionId)                                       │   │
│  │                                                                   │   │
│  │  api.knowledgeBase                                               │   │
│  │  ├── list()                                                       │   │
│  │  ├── get(id)                                                      │   │
│  │  ├── create(data)                                                 │   │
│  │  ├── update(id, data)                                            │   │
│  │  └── delete(id)                                                   │   │
│  │                                                                   │   │
│  │  api.knowledgeDb                                                 │   │
│  │  ├── list()                                                       │   │
│  │  ├── get(id)                                                      │   │
│  │  ├── create(data)                                                 │   │
│  │  ├── update(id, data)                                            │   │
│  │  ├── delete(id)                                                   │   │
│  │  └── query(id, query)                                            │   │
│  │                                                                   │   │
│  │  api.plugin                                                       │   │
│  │  ├── list()                                                       │   │
│  │  ├── get(id)                                                      │   │
│  │  ├── create(data)                                                 │   │
│  │  ├── update(id, data)                                            │   │
│  │  ├── delete(id)                                                   │   │
│  │  └── execute(id, payload)                                        │   │
│  │                                                                   │   │
│  │  api.collection                                                   │   │
│  │  ├── list()                                                       │   │
│  │  ├── get(id)                                                      │   │
│  │  ├── create(data)                                                 │   │
│  │  ├── update(id, data)                                            │   │
│  │  └── delete(id)                                                   │   │
│  │                                                                   │   │
│  │  api.dev                                                          │   │
│  │  ├── userPermissions.list()                                      │   │
│  │  ├── llm.list()                                                  │   │
│  │  ├── jsFunction.list()                                           │   │
│  │  ├── flow.list()                                                 │   │
│  │  ├── pipeline.list()                                             │   │
│  │  ├── pythonScript.list()                                         │   │
│  │  ├── personalAccessToken.list()                                  │   │
│  │  └── knowledgeGraph.list()                                       │   │
│  │                                                                   │   │
│  │  api.rest                                                         │   │
│  │  ├── pipeline.execute(data)                                      │   │
│  │  ├── plugin.execute(data)                                        │   │
│  │  ├── knowledgeDb.query(data)                                     │   │
│  │  └── chat.send(message)                                          │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                     │
                                     │ used by
                                     ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                       REACT COMPONENTS                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  import { api } from './shared/api/apiClient';                          │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  App.jsx                                                          │   │
│  │  ├── api.auth.checkIdentity()                                    │   │
│  │  └── api.project.list()                                          │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  features/chat/hooks/useChat.js                                  │   │
│  │  ├── api.chat.send(message, projectId)                           │   │
│  │  └── api.chat.getHistory(sessionId)                              │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  features/projects/hooks/useProjects.js                          │   │
│  │  ├── api.project.list()                                          │   │
│  │  └── api.project.get(id)                                         │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  features/auth/hooks/useAuth.js                                  │   │
│  │  ├── api.auth.checkIdentity()                                    │   │
│  │  ├── api.auth.logout()                                           │   │
│  │  └── api.auth.getSsoLoginUrl()                                   │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                    SYNCHRONIZATION WITH SERVER                           │
└─────────────────────────────────────────────────────────────────────────┘

Express.js Server (Backend)          ◄──────►    Chrome Extension (Frontend)
                                                      
app.use("/sso", ssoRouter)           ◄──────►    API_ROUTES.SSO.LOGIN
app.use("/auth", identityRouter)     ◄──────►    API_ROUTES.AUTH.*
app.use("/user", userRouter)         ◄──────►    API_ROUTES.USER.*
app.use("/project", projectRouter)   ◄──────►    API_ROUTES.PROJECT.*
app.use("/dev/*", devRouters)        ◄──────►    API_ROUTES.DEV.*
app.use("/chat", chatRouter)         ◄──────►    API_ROUTES.CHAT.*
app.use("/knowledge-*", kbRouters)   ◄──────►    API_ROUTES.KNOWLEDGE_*
app.use("/plugin", pluginRouter)     ◄──────►    API_ROUTES.PLUGIN.*
app.use("/rest/*", restRouters)      ◄──────►    API_ROUTES.REST.*

            100% SYNCHRONIZED ✅

┌─────────────────────────────────────────────────────────────────────────┐
│                           DOCUMENTATION                                  │
└─────────────────────────────────────────────────────────────────────────┘

├── src/routes.ts                    (Configuration - 300 lines)
├── src/routes.example.ts            (Examples - 400 lines)
├── src/shared/api/apiClient.js      (API Client - 200 lines)
├── ROUTES_GUIDE.md                  (Documentation - 600 lines)
└── ROUTES_IMPLEMENTATION_SUMMARY.md (Summary - 400 lines)

         Total: ~2000 lines of code + documentation

┌─────────────────────────────────────────────────────────────────────────┐
│                         BENEFITS ACHIEVED                                │
└─────────────────────────────────────────────────────────────────────────┘

✅ Centralized Configuration      All routes in one place
✅ Type Safety                     TypeScript support
✅ Server Synchronization          Matches Express.js exactly
✅ Clean API Interface             api.* methods
✅ Helper Functions                URL building utilities
✅ Route Metadata                  Method, auth, CORS info
✅ Comprehensive Examples          20+ code patterns
✅ Complete Documentation          600+ lines of guides
✅ Backward Compatible             Legacy routes preserved
✅ Production Ready                Build passing, no errors

                           🚀 SYSTEM READY 🚀
